import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'don´t overthink app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
